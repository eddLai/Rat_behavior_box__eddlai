# Debug 紀錄：reward_R (pin9) 伺服不會動

> 目的：記錄所有已做過的測試與結論，避免鬼打牆。每做一步就回來更新。
> 最後更新：2026-06-22
> **狀態：✅ 已解決**（根因 = R 伺服自身 GND 地線裂焊；解法 = 把 R 伺服 GND 直接拉到 Arduino 空的 GND 腳，繞過裂焊。見 T18 / §3 / §6）

---

## 0. 系統與症狀

**系統**
- Arduino（Uno/Nano, ATmega328P）跑 [UART_left_right_dir_deter_v7.ino](../Arduino_UART_from_python/UART_left_right_dir_deter_v7/UART_left_right_dir_deter_v7.ino)
- 2× **MG90S 金屬齒**伺服當三向給水閥：
  - `reward_L` → **pin10**（正常）
  - `reward_R` → **pin9**（異常）
- 伺服電源：**外部 5V**（powerbank→TTL 輸出），經洞洞板，各自焊出 VCC/GND
- 訊號線：Arduino pin → 伺服訊號腳

**症狀**
- `reward_R`(pin9) 伺服**不會轉**（卡住嗡嗡叫 / 或無反應）。
- `reward_L`(pin10) **正常**。
- 兩路差異：只差「Arduino 的腳」和「中間的線路」。

---

## 1. 已做過的測試與結果

| # | 測試 | 結果 | 推論 |
|---|------|------|------|
| T1 | 換一顆**新伺服**到 pin9 | 反應一模一樣（不轉） | 不是這顆伺服 |
| T2 | 把壞那顆拿到**另一台 Arduino** 用簡易碼跑 | **正常轉動** | 伺服本體沒問題 |
| T3 | 伺服**從機構上拆下空轉**測 | 一樣不轉 | 與三向閥/齒輪機構無關 |
| T4 | 運轉時量伺服接頭 **VCC–GND** | 穩定 **5V** | 電源到伺服 OK |
| T5 | 量伺服接頭 **GND ↔ 共地 GND** 連通 | 蜂鳴器響（且焊點 AB 膠封死） | 共地 OK |
| T6 | pin10(L) 那路 | 正常 | 反證：Arduino GND 確實與伺服共地（否則 L 也不會動） |
| T7 | 訊號線從 pin9→pin4（v7 改 attach(4)，manual mode 按1） | 「靜靜待著」沒動 | ⚠️ 無效測試：v7 manual mode 內部又 attach(9)，且伺服未必插在被驅動的腳 |
| T8 | 用乾淨 sweep sketch 測 pin 4/6/9/11 | 無反應、訊號對 GND 無電壓；但 pin10 會動 | ⚠️ 當時「伺服訊號線沒插到被驅動的那支腳」/ 線是斷的 |
| T9 | **發現訊號線是斷的**：舊線量不到電壓 → **換新單心線** | 腳對 GND 出現正常 **0.2~0.5V 跳動** | ✅ 找到並修掉一個真故障（斷線） |
| T10 | 換新線後，量**伺服接頭**：VCC–GND=5V、訊號–GND=0.2~0.5V 跳動 | 數值正常，但**伺服仍不動** | 訊號/電源/地在伺服端都「測得到」，卻不轉 |
| T11 | 把 **R 的訊號線接到 pin10**（與 L 並接） | **連 L 也不動了！** | 🔴 **重大突破**：正常高阻抗訊號線不可能拖垮 pin10 → **R 訊號路徑某處短路/漏電到 GND**，把驅動腳拉垮。解釋了 pin9 只量到 0.3V 殘值、伺服收不到完整脈衝 |
| T12 | 把 R 訊號線從 pin10 **拔掉** | **L 仍然不動（沒恢復）** | ⚠️ 當時推測 pin10 被燒壞——**後來證實是假警報**（見 T13） |
| T13 | 重測 pin10 + reward_L | **又會動了！** | 🟢 **翻盤**：之前 L 不動的真正原因是 **powerbank 關掉了、伺服根本沒供電**。→ T11/T12 的「短路 / pin10 燒壞」結論**全部失效**，是 powerbank 沒開造成的假象 |
| T14 | 對 pin9 / pin10 / 對照腳做**靜態拉 High**（非 PWM），量腳↔GND | **全部 ≈5V** | ✅ **沒有任何腳燒掉**，pin9、pin10 都健康。reward_R 不動跟 Arduino 腳無關 |
| T15 | **交叉測試**（最關鍵）：① **原 Arduino** 控 pin9 訊號 + **另一塊 Arduino** 供 5V/GND；② **另一塊 Arduino** 控 pin9 訊號 + **原 powerbank** 供 5V | ① **會動** ✅ ② **不會動** ❌ | 🎯 **破案**：唯一跟成敗相關的變數是**電源**。用別的電源就會動 → 故障在 **powerbank → R 伺服這條供電/接地路徑**。①同時證明原 Arduino pin9 訊號 OK、R 伺服本體 OK |
| T16 | 用原 powerbank 供電時，量 R 伺服接頭 VCC↔GND | **仍有 5V，但伺服不動** | ⚠️ 不矛盾：5V 是「伺服自己 VCC↔GND」（兩腳都來自 powerbank），**證明不了伺服 GND 有跟 Arduino 共地**。最可能是 **R 那支的 powerbank-GND↔Arduino-GND 共地斷/虛接** → 伺服有電卻讀不懂訊號（次因：VCC 焊點虛接，待機量 5V、出力時塌壓） |
| T17 | **斷電**蜂鳴反覆量地線網路（同樣兩點、隔幾分鐘量兩次） | 4 條裡 3 條翻面；且出現 **A↔B 通、A↔C 通、但 B↔C 不通** 的自我矛盾 | 🔴 **實錘 intermittent 裂焊**：A=B、A=C ⟹ B=C 是靜態電路鐵律，量到 B≠C 表示接點在量測的幾秒間就忽通忽斷。→ 任何單次通斷讀數都不可信，逐點追是無底洞 |
| T18 | **把 R 伺服 GND 直接拉一條新線到 Arduino 空的 GND 腳**（繞過 R 自身那段裂焊地線） | ✅ **馬達立刻會動** | 🎯🎯 **結案**：根因 = **R 伺服自身的 GND 地線（焊到洞洞板那段）裂焊**。新線繞過它，一次補上①訊號共地 ②回流路徑（Arduino GND 本就連到 powerbank GND，即 L 一直能動的同一條路）→ 迴路閉合，R 轉動 |

---

## 2. 已排除（高信心）

- ❌ **伺服本體**：T1（換新一樣壞）+ T2（壞的拿去別台會動）+ T15①（換電源就會動）
- ❌ **機構（三向閥/齒輪）**：T3（空轉測）
- ❌ **Arduino pin9 / pin10**：T14 靜態拉 High 全部 ≈5V，沒有腳燒掉
- ❌ **原 Arduino pin9 訊號**：T15① 用原 Arduino 控訊號就會動
- ❌ **共地（整體）**：T5 + T6 + L 正常
- ❌ **純軟體 bug**：軟體問題會讓兩側都壞，但 pin10/L 正常
- ✅ **已定位真因**：**R 伺服自身 GND 地線裂焊**（T18 拉新線繞過就會動）
- 🟢 **已推翻的舊結論**：T11「R 訊號短路」、T12「pin10 燒壞」——powerbank 沒開造成的假象（T13）；舊 §3「powerbank↔Arduino 共地斷」——T17 intermittent 假讀數誤導，真因在 R 尾線地

---

## 3. 根因（✅ 已確認，T18 結案）

🎯🎯 **R 伺服自身的 GND 地線（從伺服尾線焊到洞洞板那一段）是裂焊（intermittent）**，導致 R 的地時通時斷。
- 後果一（回流）：R 吃 powerbank 的 5V，電流要回 powerbank GND；地線裂焊 → 迴路時斷 → 沒電流 → 不轉。
- 後果二（訊號參考）：R 的 GND 飄 → 伺服讀 PWM 沒有穩定共同基準 → 讀到垃圾。
- 這完美解釋了 T16「量到 5V 卻不動」（5V 是 powerbank 自參考，跟「地通不通」無關）與 T17「量測自我矛盾/不可重現」（裂焊接點忽通忽斷）。

**為什麼 L 一直好、R 一直壞**：L 的地線那段焊點剛好咬得住、R 那段裂了。Arduino GND ↔ powerbank GND 這條共地是通的（L 靠它運作），所以 R GND 只要接到 Arduino GND 就同時拿到「共地 + 回 powerbank 的路」。

> 註：先前 §3 舊版推測「powerbank-GND↔Arduino-GND 共地斷」是被 T17 那些 intermittent 假讀數誤導，實際斷點在 **R 伺服自己的尾線地**。保留此註以免重蹈。

---

## 4. 定位斷點用的測試（已執行，留存供日後參考；結論見 T17/T18）
> 下列 C/D/E 是當時用來定位斷點的測試。實際上 E 量到的是 intermittent 假讀數（T17），最後靠 T18「直接拉新線繞過就會動」確認真因。日後再遇供電/接地問題可重用這幾招。

### ✅ 測試 C（最直接、只用電壓）：量「共地」是否斷
正常接法（powerbank 供電 + 原 Arduino 訊號），**通電**，量 **R 伺服 GND 腳 ↔ 原 Arduino GND 腳**：
- **= 0V** → 有共地（問題偏向 VCC 虛接，做測試 D）
- **不是 0V（浮動）** → 🎯 **共地斷了，就是這個**

### ✅ 測試 D（查 VCC 虛接）：帶負載量電壓
下指令叫 R 動的**當下**（伺服在吃電流時），量 R 伺服接頭 VCC↔GND：
- 待機 5V、**一動就掉到 3V 以下** → VCC 焊點虛接。

### ✅ 測試 E（斷電量通斷，定位斷點）
Arduino 拔線、powerbank 關，蜂鳴/電阻檔：
- powerbank 5V輸出 ↔ R 伺服 VCC 腳：應 ~0Ω
- powerbank GND輸出 ↔ R 伺服 GND 腳：應 ~0Ω
- powerbank GND ↔ Arduino GND（共地）：應 ~0Ω
→ 哪條「不通」就是斷點。

---

## 5. 重要方法論（避免再鬼打牆）

1. **程式 `SERVO_PIN` 必須等於「伺服訊號線實際插的那支腳」**，否則一定測不出。
2. **三用電表量 PWM 只有平均值**（0.2~0.5V 是正常），**看不出脈衝振幅/品質**。要查振幅用「靜態拉 High」測試。
3. **不要用 v7 / manual mode 測 pin 腳**：v7 多處散落 `attach(9)`，manual mode 只驅動 reward_R，會互相干擾。診斷一律用乾淨的 [servo_sweep_pintest.ino](../Arduino_UART_from_python/motor_test/servo_sweep_pintest/servo_sweep_pintest.ino)。
4. 用「**會動 / 不會動**」的二元測試（測試 A）優先於需要解讀數字的測試。
5. **「量到 5V 卻不動」一點都不矛盾**（T16 的核心教訓，務必記住）：
   - **電表量電壓幾乎不吃電流**（內阻 ~10MΩ）。所以就算 VCC 焊點裂到只剩高電阻，伺服**待機**(~10mA)時電表照樣讀到 5V；但伺服一**出力**(~300mA)就被那個爛焊點吃掉電壓 → brown-out 不動。比喻：快斷的吸管不喝時「水壓滿滿」，真的吸卻只剩一滴。→ 要查 VCC 虛接,得在**伺服出力的當下帶負載量電壓**,看會不會塌。
   - **量到的 5V 是「伺服自己 VCC↔GND」,兩腳都來自 powerbank,自成一個參考系**。它**證明不了伺服的 GND 有跟 Arduino 共地**。若共地斷了,伺服有 5V 電、卻是拿「自己浮動的地」去讀那個「相對 Arduino 地」的 PWM → 沒有共同基準 → 讀到垃圾 → 不動。→ 要查共地,得量**伺服 GND 腳 ↔ Arduino GND 腳**(通電量電壓應=0V,或斷電量通斷)。
   - 一句話:**伺服端 VCC↔GND 有 5V，只代表「powerbank 把 5V 送到了那兩腳」，不代表「能出力」也不代表「跟訊號共地」。** 這就是本案 R 故障(VCC 虛接 + 共地斷)被電表讀數誤導的根源。
6. **通斷/電阻只能「斷電」量**，通電量到的全是被真實電壓干擾的假數據（本案 Set 1 通電量 vs Set 2 斷電量結果相反，差點被帶歪）。且要量在**源頭裸金屬**（剝皮線芯/端子），別量在 AB 膠封死的點。
7. **自我矛盾 / 不可重現的通斷讀數 = intermittent 裂焊的鐵證**（T17：A↔B 通、A↔C 通、卻 B↔C 不通，靜態電路不可能）。遇到這種就**別再逐點追**（無底洞），直接判定接點裂焊、重拉乾淨線繞過。用 **wiggle test**（撥動線看讀數跳不跳）快速確認 intermittent。
8. **電流一定要繞一圈回到它的電源**：伺服吃 powerbank 的電，電流就**必須回得到 powerbank GND** 才成迴路。Arduino GND 腳能當回流點，只因它接得到 powerbank GND（它是中繼，不是終點）。診斷供電不通時，先問「電流回得去電源嗎」。

---

## 6. 修復（✅ 已實施，T18）
**核心：故障在 R 伺服自身地線的手焊裂焊段，不是 Arduino、不是訊號線、不是伺服。**

**已做的解法（會動了）**：
- 把 **R 伺服 GND 直接拉一條新線到 Arduino 空的 GND 腳**，繞過裂掉的舊地線。一條線同時補上「訊號共地」+「回流路徑」→ R 轉動。
- **為什麼一條線就夠**：電流要繞一圈回它的電源(powerbank)。Arduino 所有 GND 腳內部相通，且 Arduino GND 本就連到 powerbank GND（L 靠的同一條路）→ `R GND → Arduino GND → powerbank GND` 迴路閉合。Arduino GND 腳是「中繼站」，能用是因為它接得到 powerbank GND。

**長久穩定建議（optional，看要不要一勞永逸）**：
- 現在 R 的回流仍**借道那個 powerbank↔Arduino 的裂焊點**——它現在通、但哪天再裂 R/L 會再罷工。要根治可**補一條實線 `powerbank GND ── 主控 Arduino GND`**，把這個中繼也變成不裂的實線。
- **更乾淨的接地拓樸（star ground）**：共同點選 **powerbank GND**（或端子台），`R 伺服 GND → powerbank GND`（粗線、走回流，不經過 Arduino）、`主控 Arduino GND → powerbank GND`（訊號參考、幾乎不過電流）。避免伺服大電流穿過 Arduino 地造成雜訊。若跑實驗時 Arduino 不會抖/重啟，現狀也可接受。
- **新線一定要做應力釋放**（束線帶固定），否則它會變成下一個被扯裂的接點。
- 共地參考必須是**送 pin9 訊號的那顆 Arduino**，不能用別塊板子的 GND。
- **不需要**：換板、換伺服、改 v7 腳號、動訊號線（pin9 與訊號都已證實 OK）。
- **長遠**：powerbank 供電全面改成接線端子 + 應力釋放，淘汰 AB 膠封死的手焊點；考慮換穩定 5V 電源（powerbank 可能有低載自動斷電）。

**驗收標準**：斷電量共地三點(powerbank GND / Arduino GND / R GND)兩兩 ~0Ω 且 wiggle 撥線不跳；通電量兩兩 ≈0V；連續跑幾輪完整實驗 R 每次都穩定動作。

---

## !!! 筆記 !!!
### 一個核心觀念:電流一定要「繞一圈回到它的電源」
馬達的電是 **powerbank** 給的。所以馬達要轉,電流必須走完一個完整迴路:
```
powerbank 5V → 馬達 → 馬達GND →（某條路）→ 回到 powerbank GND
```
重點在最後那一步:**電流最終一定要回到 powerbank 的 GND** (因為電是它出的)。Arduino 的 GND 腳**本身不是終點**,它只是一個「中繼站」。

你把馬達 GND 接到 Arduino 空的 GND 腳會動,是因為:
- Arduino 板上**所有 GND 腳內部都是相通的**,而且
- 你的 **Arduino GND 又有連到 powerbank GND** (這也是 reward_L 一直能動的同一條路)

所以電流走: ```馬達GND → Arduino GND腳 → Arduino內部地 → (接點) → powerbank GND,繞回 powerbank 了 → 馬達轉```。

換句話說:不是 Arduino GND 腳有什麼魔力,是它「接得到 powerbank GND」。如果哪天 Arduino 跟 powerbank 完全沒共地,你接再多空的 Arduino GND 腳,馬達一樣不會動(電流回不去 powerbank)。